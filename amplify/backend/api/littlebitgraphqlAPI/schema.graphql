type Sample @model @auth(rules: [{allow: public, provider: apiKey}]) {
  id: ID!
  name: String!
  user_id: String
  file: S3Object
}

type S3Object {
  bucket: String!
  key: String!
  region: String!
}
 